# Configurações para a extensão YouTube Agent
# Copie este arquivo para .env e preencha os valores necessários

# Chave da API Gemini (obrigatória para transcrição e geração de SEO)
# Obtenha uma chave em https://ai.google.dev/
GEMINI_API_KEY=sua_chave_api_aqui

# Modelos Gemini preferidos (em ordem de preferência)
# Você pode modificar esta lista de acordo com suas necessidades
GEMINI_PREFERRED_MODELS=gemini-1.5-pro,gemini-pro,gemini-1.0-pro

# Configurações de transcrição
# Idioma padrão para transcrição (pt-BR, en-US, etc.)
TRANSCRIPTION_LANGUAGE=pt-BR

# Configurações de detecção de silêncio (para processamento de áudio)
# Duração mínima do silêncio a ser considerado (ms)
MIN_SILENCE=500

# Limiar para detecção de silêncio (dB)
SILENCE_THRESHOLD=-40

# Estilo padrão para geração de SEO
# Opções: clickbait, professional, educational, neutral
DEFAULT_SEO_STYLE=clickbait

# Nota: Estas configurações também podem ser definidas nas configurações do VS Code
# Vá para File > Preferences > Settings > Extensions > Youtube Agent 